package main

import "fmt"

func main(){
	var integer int

	fmt.Scanln(&integer)
	if integer >=1 && integer <=100 {
		for i := 1; i <= integer; i++{ 
		fmt.Println(i, "abracadabra")
		}	
	} else {
		fmt.Println(integer, "false")
	}
}
